@model A_Gde_Si_Ti_Pub.Models.Korisnik
@{
    ViewBag.Title = "Kreiraj Novog Korisnika";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Kreiraj Novog Korisnika</h2>

<p>Kreirajte novi korisnički nalog (kao 'Korisnik').</p>

@using (Html.BeginForm("KreirajKorisnika", "Admin", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="form-group">
        @Html.LabelFor(m => m.Username, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Username, new { @class = "form-control", required = "required", placeholder = "Korisničko ime (npr. novikorisnik)" })
            @Html.ValidationMessageFor(m => m.Username, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Email, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control", type = "email", required = "required", placeholder = "Email (npr. novi@example.com)" })
            @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.Password, new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.PasswordFor(m => m.Password, new { @class = "form-control", id = "Password", required = "required", placeholder = "Lozinka (min. 6 karaktera)" })
            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
        </div>
    </div>

    <div class="form-group">
        <label class="control-label col-md-2">Potvrdi Lozinku</label>
        <div class="col-md-10">
            <input type="password" name="potvrdaLozinke" id="potvrdaLozinke" class="form-control" required="required" placeholder="Ponovi lozinku" />
            @Html.ValidationMessage("potvrdaLozinke", "", new { @class = "text-danger", id = "confirmError" })
        </div>
    </div>

    <!-- Uloga fixed to "Korisnik" -->
    <div class="form-group">
        <label class="control-label col-md-2">Uloga</label>
        <div class="col-md-10">
            <p class="form-control-static">Korisnik (automatski dodeljeno)</p>
        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Kreiraj Korisnika" class="btn btn-success" />
            @Html.ActionLink("Nazad", "UpravljajKorisnicima", null, new { @class = "btn btn-secondary" })
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script>
        $(function () {
            $('#PotvrdaLozinke').on('input', function () {
                var password = $('#Password').val();
                var confirm = $(this).val();
                var errorDiv = $('#confirmError');
                if (password !== confirm && confirm.length > 0) {
                    errorDiv.text('Lozinke se ne poklapaju.');
                } else {
                    errorDiv.text('');
                }
            });
        });
    </script>
}
